from setuptools import setup, find_packages

setup(
    name='Reverse-Multilingual-Typo',  # Replace with your project's name
    version='0.1.0',  # Your project's version
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here
        # e.g., 'requests', 'numpy'
    ],
    # Additional fields such as author, author_email, description, etc.
)